# OOP_Bank-Management-System
A Python-based banking application that manages multiple accounts and supports account creation, deposits, withdrawals, money transfers, PIN authentication, balance checking, and transaction history. It uses OOP, exception handling, data validation, and JSON for data storage.
